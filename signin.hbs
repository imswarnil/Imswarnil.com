{{!< default}}

{{!-- /signin — combined tabbed auth (sign in active). Markup lives in
     partials/components/auth.hbs; /signup renders the same with signup active. --}}
{{> "utility/auth" tab="signin"}}
