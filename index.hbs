{{!< default}}

{{!-- Fallback listing — rarely hit (routes send traffic to /blog, /archive etc.) --}}
{{> "utility/collection-header" kicker="Index" title="Latest posts"}}

<section class="container-site py-12">
	<div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
		{{#foreach posts}}
			{{> "blog/card"}}
		{{/foreach}}
	</div>
	{{> "utility/pagination"}}
</section>
