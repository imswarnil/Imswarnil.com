{{!< default}}
{{!-- The author archive. Ghost puts the author on the page as `author`, so the
     fields are read off it directly: `{{#author}}` is the deprecated block form
     and gscan errors on it. Note the prefix — bare `{{name}}` resolves to
     nothing here, which renders an empty heading rather than a failure. --}}

{{> page-header kicker="Author" title=author.name description=author.bio}}
{{> list}}
