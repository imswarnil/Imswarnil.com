{{!-- Markup for the {{navigation}} helper. --}}
{{#foreach navigation}}
	<a href="{{url}}" class="text-sm text-zinc-600 hover:text-zinc-900 dark:text-zinc-400 dark:hover:text-zinc-100{{#if current}} font-medium text-zinc-900 dark:text-zinc-100{{/if}}">{{label}}</a>
{{/foreach}}
