{{!-- Long-form content. `.content` (design system, 31-content) styles every
     heading, list, quote, table and Koenig card inside {{content}}, so no
     template ever styles editor output itself. --}}
<div class="content">
	{{content}}
</div>
