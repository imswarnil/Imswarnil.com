{{!-- Newsletter issue: read exactly as it was sent. --}}
<article class="container-prose section">
	<header class="stack u-mb-8">
		<span class="eyebrow"><svg class="icon" aria-hidden="true"><use href="#i-mail"/></svg> Issue</span>
		<h1 class="t-h1">{{title}}</h1>
		{{> util/meta}}
	</header>

	{{> util/feature-image bleed=true}}
	{{> post-body}}
	{{> util/pager}}
</article>

{{> util/cta}}
