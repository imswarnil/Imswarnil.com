
<style>
    /* --- 1. LAYOUT CONTAINER --- */
    .lesson-viewer-container {
        max-width: 1400px; /* Wider for "Classroom" feel */
        margin: 0 auto;
        padding: var(--spacing-6) var(--layout--padding);
        display: grid;
        grid-template-columns: 1fr;
        gap: var(--spacing-9);
    }

    @media (min-width: 1024px) {
        .lesson-viewer-container {
            grid-template-columns: 1fr 380px; /* Video Left, Syllabus Right */
            align-items: start;
        }
    }

    /* --- 2. MAIN CONTENT (LEFT) --- */
    .lesson-main-area {
        display: flex;
        flex-direction: column;
        gap: var(--spacing-6);
    }

    /* Cinema Mode Media */
    .lesson-player-wrapper {
        width: 100%;
        background: #000;
        border-radius: var(--radius-4);
        overflow: hidden;
        box-shadow: 0 10px 30px rgba(0,0,0,0.15);
        aspect-ratio: 16/9;
        position: relative;
    }

    .lesson-player-wrapper img,
    .lesson-player-wrapper video,
    .lesson-player-wrapper iframe {
        width: 100%;
        height: 100%;
        object-fit: cover;
        position: absolute;
        inset: 0;
    }

    /* Header Info */
    .lesson-header-info h1 {
        font-family: var(--font-family-headings);
        font-size: clamp(1.5rem, 2.5vw, 2.2rem);
        margin: 0 0 var(--spacing-3) 0;
        line-height: 1.2;
    }

    .lesson-breadcrumb {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 0.85rem;
        color: var(--color-secondary);
        margin-bottom: var(--spacing-4);
        text-transform: uppercase;
        letter-spacing: 0.5px;
        font-weight: 600;
    }
    
    .lesson-breadcrumb a { text-decoration: none; color: var(--color-secondary); }
    .lesson-breadcrumb a:hover { color: var(--ghost-accent-color); }

    /* Content Body */
    .lesson-body {
        font-size: 1rem;
        line-height: 1.7;
        max-width: 800px; /* Readable width inside the full container */
    }

    /* --- 3. STEPPER SIDEBAR (RIGHT) --- */
    .lesson-sidebar {
        position: sticky;
        top: 100px; /* Adjust for navbar */
        background: var(--color-background);
        border: 1px solid var(--color-border);
        border-radius: var(--radius-4);
        display: flex;
        flex-direction: column;
        overflow: hidden;
        max-height: calc(100vh - 140px);
    }

    .sidebar-header {
        padding: 20px;
        background: var(--color-background-100);
        border-bottom: 1px solid var(--color-border);
    }
    .sidebar-header h4 { margin: 0; font-size: 0.9rem; text-transform: uppercase; color: var(--color-secondary); }
    .sidebar-course-title { font-weight: 700; color: var(--color-contrast); display: block; margin-top: 5px; }

    /* Stepper List Styling */
    .stepper-list {
        list-style: none;
        padding: 20px 20px 20px 10px; /* Less padding left for line */
        margin: 0;
        overflow-y: auto;
    }

    .stepper-item {
        position: relative;
        padding-left: 35px; /* Space for dot/line */
        margin-bottom: 0;
    }

    /* The Vertical Line */
    .stepper-item::before {
        content: '';
        position: absolute;
        left: 14px;
        top: 30px; /* Start after dot */
        bottom: -15px; /* Extend to next */
        width: 2px;
        background: var(--color-border);
    }
    .stepper-item:last-child::before { display: none; }

    .stepper-link {
        display: block;
        padding: 12px 15px;
        border-radius: var(--radius-2);
        text-decoration: none !important;
        color: var(--color-foreground);
        transition: background 0.2s;
        position: relative;
    }

    .stepper-link:hover {
        background: var(--color-background-100);
        color: var(--ghost-accent-color);
    }

    /* The Dot (Step Number) */
    .step-number {
        position: absolute;
        left: -35px; /* Pull back into padding */
        top: 12px;
        width: 28px;
        height: 28px;
        border-radius: 50%;
        background: var(--color-background);
        border: 2px solid var(--color-border);
        color: var(--color-secondary);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.75rem;
        font-weight: 700;
        z-index: 2;
    }

    /* Active State */
    .stepper-item.active .step-number {
        background: var(--ghost-accent-color);
        border-color: var(--ghost-accent-color);
        color: #fff;
    }
    .stepper-item.active .stepper-link {
        background: var(--color-background-100);
        font-weight: 600;
        color: var(--color-contrast);
    }

    /* --- 4. STRICT NAVIGATION (Bottom) --- */
    .lesson-navigation {
        margin-top: var(--spacing-8);
        padding-top: var(--spacing-6);
        border-top: 1px solid var(--color-border);
        display: flex;
        justify-content: space-between;
        gap: 20px;
    }

    .nav-card {
        flex: 1;
        border: 1px solid var(--color-border);
        border-radius: var(--radius-3);
        padding: 20px;
        text-decoration: none;
        transition: all 0.2s;
        display: flex;
        flex-direction: column;
        gap: 5px;
    }

    .nav-card:hover {
        border-color: var(--ghost-accent-color);
        background: var(--color-background-100);
    }

    .nav-label { font-size: 0.75rem; text-transform: uppercase; color: var(--color-secondary); font-weight: 700; }
    .nav-title { font-size: 1.1rem; font-weight: 700; color: var(--color-contrast); line-height: 1.3; }
    .nav-card-prev { text-align: left; }
    .nav-card-next { text-align: right; }

    /* Mobile Fixes */
    @media (max-width: 1023px) {
        .lesson-sidebar { position: static; max-height: none; margin-top: 40px; }
        .lesson-player-wrapper { order: -1; } /* Ensure video is first on mobile */
    }
</style>

<div class="lesson-viewer-container">

    {{!-- 1. LEFT COLUMN: Media, Header, Content, Nav --}}
    <div class="lesson-main-area">
        
        {{!-- Breadcrumb: Find Parent Course --}}
        {{#get "posts" filter="tag:hash-courses-col+tag:{{primary_tag.slug}}" include="tags" limit="1"}}
            {{#foreach posts}}
            <nav class="lesson-breadcrumb">
                <a href="{{url}}">← Back to {{title}}</a>
            </nav>
            {{/foreach}}
        {{/get}}

        {{!-- Lesson Title --}}
        <div class="lesson-header-info">
            <h1>{{title}}</h1>
        </div>

        {{!-- Cinema Mode Media Player --}}
        <div class="lesson-player-wrapper">
            {{> "post/media"}}
        </div>

        {{!-- Content Text --}}
        <div class="lesson-body">
            {{content}}
        </div>

        {{!-- STRICT NAVIGATION (Next/Prev within Course Only) --}}
        <div class="lesson-navigation">
            
            {{!-- PREVIOUS LESSON LOGIC --}}
            {{#get "posts" 
                filter="tag:hash-lessons-col+tag:{{primary_tag.slug}}+published_at:<'{{published_at}}'" 
                limit="1" 
                order="published_at desc"
            }}
                {{#if posts}}
                    {{#foreach posts}}
                    <a href="{{url}}" class="nav-card nav-card-prev">
                        <span class="nav-label">← Previous Lesson</span>
                        <span class="nav-title">{{title}}</span>
                    </a>
                    {{/foreach}}
                {{else}}
                    <div style="flex:1;"></div> {{!-- Spacer if no prev --}}
                {{/if}}
            {{/get}}

            {{!-- NEXT LESSON LOGIC --}}
            {{#get "posts" 
                filter="tag:hash-lessons-col+tag:{{primary_tag.slug}}+published_at:>'{{published_at}}'" 
                limit="1" 
                order="published_at asc"
            }}
                {{#if posts}}
                    {{#foreach posts}}
                    <a href="{{url}}" class="nav-card nav-card-next">
                        <span class="nav-label">Next Lesson →</span>
                        <span class="nav-title">{{title}}</span>
                    </a>
                    {{/foreach}}
                {{else}}
                    {{!-- If no next lesson, show "Finish Course" --}}
                    {{#get "posts" filter="tag:hash-courses-col+tag:{{primary_tag.slug}}" limit="1"}}
                        {{#foreach posts}}
                        <a href="{{url}}" class="nav-card nav-card-next" style="border-color:var(--color-success);">
                            <span class="nav-label" style="color:var(--color-success);">Complete</span>
                            <span class="nav-title">Back to Course Home</span>
                        </a>
                        {{/foreach}}
                    {{/get}}
                {{/if}}
            {{/get}}

        </div>

        {{!-- Comments --}}
        <div style="margin-top: 40px;">
            {{> "post/comments"}}
        </div>

    </div>

    {{!-- 2. RIGHT COLUMN: STEPPER SIDEBAR --}}
    <aside class="lesson-sidebar">
        
        {{!-- Sidebar Header: Parent Course --}}
        {{#get "posts" filter="tag:hash-courses-col+tag:{{primary_tag.slug}}" limit="1"}}
            {{#foreach posts}}
            <div class="sidebar-header">
                <h4>Course Curriculum</h4>
                <a href="{{url}}" class="sidebar-course-title">{{title}}</a>
            </div>
            {{/foreach}}
        {{/get}}

        {{!-- Stepper List --}}
        {{#get "posts" 
            filter="tag:{{primary_tag.slug}}+tag:hash-lessons-col" 
            include="tags" 
            order="published_at asc" 
            limit="100" 
            as |lessons|
        }}
            {{#if lessons}}
                <ul class="stepper-list">
                    {{#foreach lessons}}
                        {{!-- Check if this is the current post --}}
                        <li class="stepper-item {{#if (eq id ../../id)}}active{{/if}}">
                            <a href="{{url}}" class="stepper-link">
                                <span class="step-number">
                                    {{#if (eq id ../../id)}}
                                        {{!-- Play Icon for Active --}}
                                        <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>
                                    {{else}}
                                        {{@number}}
                                    {{/if}}
                                </span>
                                {{title}}
                                
                                {{!-- Optional: Lock Icon if no access --}}
                                {{#unless access}}
                                    <svg style="width:12px; color:var(--color-mute); margin-left:5px;" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect><path d="M7 11V7a5 5 0 0 1 10 0v4"></path></svg>
                                {{/unless}}
                            </a>
                        </li>
                    {{/foreach}}
                </ul>
            {{/if}}
        {{/get}}

    </aside>

</div>