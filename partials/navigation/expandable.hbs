{{!--
	Collapse or Dropdown.

	Info: </div> and </ul> are closed automatically in the navigation.hbs.

	Variables:
		- nav: data included in {{navigation}}.
		- item: data included in the link.
		- this: all data.
--}}

{{#if open_tag}}
	{{!-- Dropdown. --}}
	{{#if nav.dropdown}}
		<div
			class="dropdown"
			role="menu"
			aria-labelledby="{{concat "nav-" nav.name "-dropdown-toggle-" item.slug "-" @index}}"
			id="{{concat "nav-" nav.name "-dropdown-content-" item.slug "-" @index}}"
		>
			<ul>

	{{!-- Collapse. --}}
	{{else}}
		<div
			class="collapse"
			role="menu"
			aria-labelledby="{{concat "nav-" nav.name "-collapse-toggle-" item.slug "-" @index}}"
			id="{{concat "nav-" nav.name "-collapse-content-" item.slug "-" @index}}"
		>
			<ul>
	{{/if}}

{{else}}
	</ul></div>
{{/if}}
