b-cov
*.seed
*.log
*.csv
*.dat
*.out
*.pid
*.gz
*.zip

pids
logs
results

npm-debug.log
node_modules

.idea/*
*.iml
projectFilesBackup

.DS_Store

dist/

# assets/built/screen.css IS committed. It is compiled from the Creator Design
# System, which is a local authoring-time dependency — the deploy has no way to
# rebuild it, so the artefact ships with the theme. Run `npm run build` and
# commit the result whenever assets/css/ or the system changes.
assets/built/main.js
