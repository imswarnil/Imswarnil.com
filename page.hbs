{{!< default}}

{{#post}}
	<article class="mx-auto max-w-3xl px-4 py-10">
		{{#if @page.show_title_and_feature_image}}
			<header class="mb-8">
				<h1 class="text-3xl font-semibold leading-tight tracking-tight sm:text-4xl">{{title}}</h1>
			</header>

			{{#if feature_image}}
				<img src="{{img_url feature_image size="l"}}" alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}" class="mb-8 w-full rounded-lg" width="1000" height="563" />
			{{/if}}
		{{/if}}

		<div class="prose prose-zinc max-w-none dark:prose-invert">
			{{content}}
		</div>
	</article>
{{/post}}
